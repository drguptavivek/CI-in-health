% Preamble for CI in Health PDF generation
\usepackage{pdflscape}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{fontspec}

% Set main font to support Hebrew, Cyrillic, and other Unicode characters
\setmainfont{Arial Unicode MS}
\setmonofont{Menlo} % Optional: set a mono font that might support box-drawing

\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % Clear all header and footer fields
\fancyfoot[C]{\thepage} % Centered page number in footer
\renewcommand{\headrulewidth}{0pt} % Remove header line
\renewcommand{\footrulewidth}{0pt} % Remove footer line
